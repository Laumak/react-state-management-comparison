# Automatically install missing peer dependencies instead of just warning
auto-install-peers=true

# Keep packages inside their own node_modules rather than hoisting everything
# to the root — enforces cleaner dependency boundaries between workspace packages
shamefully-hoist=false

# Allow installation to succeed even when peer dependency version ranges conflict;
# useful while the workspace is still being set up
strict-peer-dependencies=false
